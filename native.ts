// This is a temporary file for backward compatibility with 'styled-components/native'.

import styled from './index'

export { styled, css, ThemeProvider, ThemeContext } from './index'

export default styled
